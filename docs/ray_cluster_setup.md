# คู่มือการตั้งค่า Ray Cluster (Laptop + Mining Rig)

เอกสารนี้อธิบายวิธีการเชื่อมต่อ **Notebook แรงสูง (Core Ultra 7 / 32GB RAM)** เข้ากับ **Mining Rig (8x RTX 3060)** เพื่อสร้าง "Super Computer" ขนาดย่อมโดยใช้ **Ray Framework**

## 🎯 คอนเซปต์การทำงาน
*   **Head Node (Laptop)**: รับหน้าที่เป็น "สมอง" คอยจัดการคิวงาน, ประมวลผล Logic ที่ซับซ้อน, และรัน Python Script
*   **Worker Node (Mining Rig)**: รับหน้าที่เป็น "กล้ามเนื้อ" เน้นใช้ GPU 8 ใบในการรันโมเดล AI เพียงอย่างเดียว
*   **ประโยชน์**: ช่วยลดภาระ CPU/RAM ของ Mining Rig ที่มีจำกัด (Pentium G4400 / 16GB RAM) ทำให้ระบบโดยรวมลื่นไหลขึ้น

---

## 🛠️ 1. การเตรียมพร้อม (ทำทั้ง 2 เครื่อง)
ต้องติดตั้ง Python และ Ray ให้เวอร์ชั่นตรงกันเป๊ะๆ (แนะนำ Python 3.10)

```bash
# ติดตั้ง Ray พร้อม Dashboard
pip install "ray[default]"
```

> **ข้อควรระวัง**: ทั้ง 2 เครื่องต้องอยู่ในวง LAN เดียวกัน และสามารถ Ping หากันเจอ

---

## 🚀 โหมด A: เมื่อมี Laptop (Cluster Mode) - ประสิทธิภาพสูงสุด
ใช้เมื่อท่านทำงานอยู่ที่บ้านและเปิด Laptop ทิ้งไว้

### ฝั่ง Laptop (Head Node)
1.  เปิด Terminal แล้วรันคำสั่ง:
    ```bash
    ray start --head --port=6379 --dashboard-host=0.0.0.0
    ```
2.  ท่านจะได้ข้อความคล้ายๆ นี้ (จดไว้):
    > `ray start --address='192.168.1.XX:6379' --redis_password='...'`

### ฝั่ง Mining Rig (Worker Node)
1.  **สำคัญมาก**: ต้อง **ปิด vLLM ตัวเดิม** ที่รันอยู่ก่อน (เพราะมันจอง GPU ไว้)
    ```bash
    pkill -f vllm
    ```
2.  เชื่อมต่อเข้ากับ Laptop:
    ```bash
    ray start --address='192.168.1.XX:6379' --num-gpus=8
    ```

### การใช้งาน
เขียน Python Script บน Laptop โดยระบุให้ไปรันบน Cluster:
```python
import ray
ray.init(address='auto') # เชื่อมต่อกับ Cluster อัตโนมัติ

# โค้ดที่เหลือจะถูกกระจายไปทำงานบน Rig เอง
```

---

## 🏠 โหมด B: เมื่อไม่มี Laptop (Standalone Mode) - เน้นความชัวร์
ใช้เมื่อต้องพก Laptop ออกไปข้างนอก หรือต้องการเปิด Server ทิ้งไว้ 24/7

### สิ่งที่ต้องทำ
1.  **บน Mining Rig**: ต้องยกเลิกการเชื่อมต่อกับ Ray (ถ้าเคยต่อไว้)
    ```bash
    ray stop
    ```
2.  **รัน vLLM แบบปกติ**: ใช้สคริปต์ `start_vllm.sh` ที่เราทำไว้
    ```bash
    ./start_vllm.sh
    ```

---

## ⚠️ ข้อจำกัดและคำเตือน (สำคัญมาก)

1.  **การสลับโหมดต้อง "Reboot ระบบ AI" เสมอ**
    *   ท่าน **ไม่สามารถ** ถอด Laptop ออกแล้วให้ Mining Rig ทำงานต่อได้ทันที
    *   ทุกครั้งที่จะเปลี่ยนโหมด ต้อง **ปิดโปรแกรม** และ **เริ่มรัน vLLM ใหม่** (ซึ่งต้องรอโหลดโมเดล 40-50 นาที)

2.  **แนะนำ Work Flow ที่ดีที่สุด**
    *   หากท่านไม่ได้จะเทรนโมเดล หรือรัน Logic ที่ซับซ้อนมากๆ **แนะนำให้ใช้โหมด B (Standalone) เป็นหลัก**
    *   ใช้ Laptop เป็นเพียง **Client** (ผู้เรียกใช้) โดยยิง Request ผ่าน API (`http://IP_RIG:8000`)
    *   วิธีนี้ท่านสามารถพก Laptop ไปไหนก็ได้ โดยที่ Server ที่ห้องไม่ล่ม และไม่ต้องรอโหลดโมเดลใหม่ครับ
